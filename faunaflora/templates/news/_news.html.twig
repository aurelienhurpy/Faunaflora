{% set url=path('news_single',{'id':news.id}) %}
    <div class="">
        <div id="portfolio">
            <a href="{{ url }}">
                <img src="{{ asset('images/fauna/' ~ news.fileName) }}" alt="" style="height:280px;width:350px;display:block;">
                <figcaption>
                    <h5 class="text-center mt-3">{{news.title}}</h5>
                    <p class="mt-3 prest text-truncate">{{news.content}}</p>
                </figcaption> 
            </a>
        </div>
    </div>







             
       
            
          