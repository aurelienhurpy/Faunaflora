 {% set url=path('prestations_single',{'id':prestations.id}) %}
    <div class="">
        <div id="portfolio">
            <a href="{{ url }}">
                <img src="{{ asset('images/fauna/' ~ prestations.fileName) }}" alt="" style="height:280px;width:350px;display:block;">
                <figcaption>
                    <h5 class="text-center mt-3 presttitle">{{prestations.title}}</h5>
                    <p class="mt-3 prest text-truncate">{{prestations.content}}</p>
                </figcaption> 
            </a>
                            
        </div>
    </div>







             
       
            
          